=24点=

==文件==
* CHECK.PAS:检查输入的数据是否有且仅有要求的数字。
* INPUT.PAS:输入数据。
* JSBDS.PAS:计算表达式（抄的书就是了）
* PRINT.PAS:打印。
* RAN.PAS:随机产生数据。
* 24.PAS:24点主程序。
* J24-I.PAS:(解24-输入)手动输入求解24点(纯搜索算法，与其他程序、库无关)。
==使用==
24.EXE
* &lt;0&gt;..&lt;9&gt;, &lt;(&gt;, &lt;)&gt;, &lt;[&gt;, &lt;]&gt;, &lt;{&gt;, &lt;}&gt;, &lt;-&gt;, &lt;+&gt;, &lt;*&gt;, &lt;/&gt;:输入(三套括号没有区别，都显示为"(", ")"且可以使用若干层)
* &lt;Enter&gt;:确定。
* &lt;Esc&gt;:退出。
J24-I.EXE
* 输入4个数，回车即可。返回的为第一个搜索到的解，并非最优解。

==其他文件==
JSBDS.PAS是抄的。
